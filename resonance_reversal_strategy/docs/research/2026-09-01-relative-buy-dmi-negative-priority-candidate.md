# RELATIVE BUY 负 DMI 优先候选预注册

## 研究身份

- 候选 build：`20260901.4`
- 对照基线：`20260828.5`
- 训练期：2019-01-01 至 2021-12-31
- 2018 数据：只读 warm-up 和交易日历
- 第一阶段：普通摩擦聚宽回测
- 状态：预注册候选，尚未获得聚宽真实路径结果，不得视为保留基线

## 只读证据

在 `.5` 普通摩擦原交易路径固定的已平仓 RELATIVE BUY 中：

- `plus_di < minus_di`：28 笔，23 胜 5 负，胜率 82.14%，净利润约 9,783.8 元；
- `plus_di >= minus_di`：25 笔，17 胜 8 负，胜率 68.00%，净利润约 516.9 元；
- 两侧 Fisher 检验约 `p=0.339`，只支持有限的候选资格，不构成规则有效性证明；
- 结果属于 `POST_BACKTEST_READ_ONLY_ATTRIBUTION` 和
  `ORIGINAL_TRADE_PATH_FIXED`，不能替代候选策略回测。

## 唯一允许变化

RELATIVE BUY 候选使用 T-1 快照中已经存在的 DMI 观察值分组：

1. `plus_di < minus_di` 的候选优先；
2. `plus_di >= minus_di`、恰好相等、缺失或非有限的候选随后；
3. 每组内部继续使用原排序：支持数降序、BOLL 新鲜度、代码；
4. 该状态只改变软排序，不删除候选，不改变空位消耗或下单结果处理。

零是唯一自然边界，不搜索或引入其他阈值。

## 保护项

- FORMAL BUY 的资格、排序和优先执行顺序不变；
- RELATIVE BUY 的资格、分支和空位补位机制不变；
- 信号、持仓上限、目标仓位、下单、卖出和 ATR 观察策略不变；
- `ATR_EXIT_POLICY="OBSERVE_ONLY"`；
- `RELATIVE_BUY_POLICY="EMPTY_SLOT_BACKFILL"`；
- 只使用 T-1 及更早数据，T 日数据不得修改排序；
- 不叠加量比、BOLL 斜率或其他候选；
- manifest、日志身份、原始证据和 fail-closed 校验不变。

## 普通摩擦验收

与 `.5` 基线相比，候选必须在聚宽真实路径中同时满足：

- 交易胜率高于 75.76%；
- 总收益不低于 66.07%；
- 最大回撤不高于 15.93%；
- 日胜率不低于 `.5`；
- 交易数量不得出现不可接受的下降；
- 三个年度均保持正贡献，年度胜率和收益不得集中恶化；
- 前 1 笔、前 3 笔毛利润集中度不得高于 `.5`；
- 期末未平仓数量不得增加且必须单独披露；
- 策略身份、T-1、训练边界和日志证据校验全部通过。

普通摩擦未通过即拒绝，不进入双倍摩擦。普通摩擦通过后，再由用户决定是否补做双倍摩擦。
